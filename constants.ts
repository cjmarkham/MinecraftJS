export const CHUNK_WIDTH = 2 // The amount of blocks in a chunk
export const CHUNK_HEIGHT = 1 // The amount of blocks high in a chunk
// The max height for terrain generation. The rest will be air blocks
export const MAX_TERRAIN_HEIGHT = 9 // the amount of blocks allowed for terrain height * 2, which is a block offset height
export const MAX_CHUNKS = 1 // The max amount of chunks to render (will be squared)
